{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Game (make)",
            "type": "shell",
            "command": "make",
            "problemMatcher": [
                "$gcc"   // enables inline VSCode error highlighting
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
