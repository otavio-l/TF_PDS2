{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "make and debug",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/game",
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "MIMode": "gdb",
            "preLaunchTask": "Build Game (make)",
            "miDebuggerPath": "/usr/bin/gdb"
        },
    ]
}
